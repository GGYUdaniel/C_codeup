#include <stdio.h>

int main()
{
    int a, sum=0, i;
    scanf("%d", &a);
    for(i=1; i<=a; i++)
    {
        if(i%2==0)
        {
            sum = sum +i;
        }
    }
    printf("%d", sum);
    
    return 0;
}